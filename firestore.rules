rules_version = '2';
// Locked down by default. Enable Auth + open specific paths only when you add a backend.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
